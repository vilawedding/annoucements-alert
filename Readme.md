# 1. Cài đặt dependencies
npm install

# 2. Cấu hình .env file
cp .env.example .env
# Sửa .env với token Telegram của bạn

# 3. Chạy bot
node index.js

# Hoặc chạy với nodemon (development)
npm run dev